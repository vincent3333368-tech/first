# --- PocketBase 核心数据 (绝对不能上传) ---
# 数据库存储在这里，包含你的账号、密码和资产数据，必须忽略
pb_data/

# --- 可执行文件 ---
# PocketBase 的主程序文件，别人可以通过 wget 自行下载，没必要上传
pocketbase
pocketbase.exe

# --- 日志与临时文件 ---
*.log
.DS_Store
thumbs.db

# --- 前端开发环境 (如果你后续使用了 npm) ---
node_modules/
dist/

# --- 敏感环境变量 ---
.env

# --- Android / Gradle build outputs ---
.gradle/
**/build/
*.apk
*.aab
